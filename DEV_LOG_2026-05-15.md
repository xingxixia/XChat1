# ZcChat2 开发日志 - 2026-05-15

## 今日目标

继续熟悉 ZcChat2 的结构，并围绕“把它改成更像 Galgame 桌面陪伴角色”的方向做一些小步验证。

今天主要处理了四件事：

1. 启动时只显示立绘。
2. 记录设置窗口越界问题，并整理 Issue 思路。
3. 生成并接入一个测试用角色立绘。
4. 初步排查配置 API Key 后角色不回话的问题。

## 1. 启动流程调整

原来的启动逻辑是：

```cpp
Dialog dialogWin;
dialogWin.show();
Tachie tachieWin;
tachieWin.show();
```

这样程序一启动，聊天框和立绘会同时出现。

今天改成：

```cpp
Dialog dialogWin;
// 对话框仍然提前创建，用于接收立绘信号和设置页配置；启动时不显示，右键立绘再打开。
// dialogWin.show();
Tachie tachieWin;
tachieWin.show();
```

文件：

`E:\File\AIuseing\xai\ZcChat2\main.cpp`

这样启动后只显示立绘，更接近桌宠体验。

注意：

- `Dialog dialogWin;` 不能删。
- 因为立绘右键打开聊天框依赖已有信号槽：

```cpp
QObject::connect(&tachieWin, &Tachie::requestToggleVisible, &dialogWin,
                 &Dialog::ToggleVisible);
```

也就是说，聊天框对象仍然存在，只是启动时不显示。

## 2. Release 构建验证

直接在普通 PowerShell 里构建时，会报：

```text
fatal error C1083: 无法打开包括文件: “type_traits”: No such file or directory
```

原因是没有加载 VS C++ 编译环境。

使用 VS2022 的 `vcvars64.bat` 后构建成功。

可用命令：

```powershell
cd E:\File\AIuseing\xai\ZcChat2
& cmd.exe /c 'call "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat" && "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe" --build .\build-release --config Release'
```

继续使用 Release，不建议 Debug。

## 3. 设置窗口越界问题

用户发现：

当对话框比较高，或者靠近屏幕上边缘时，打开设置窗口会出现：

- 设置窗口顶部超出屏幕。
- 标题栏和关闭按钮不可见。
- 窗口层级较高，导致不好关闭，也不好操作其它窗口。

后续确认：

原作者发布的软件也能复现这个问题，不是本地二次开发引入的。

可疑代码：

`main.cpp`

```cpp
settings = new MainWindow(&dialogWin, &tachieWin, &dialogWin);
```

第三个参数 `&dialogWin` 是设置窗口的 parent。

而 Dialog 本身在 `windows/dialog/dialog.cpp` 中设置了：

```cpp
Qt::FramelessWindowHint | Qt::Tool | Qt::WindowStaysOnTopHint
```

推测：

设置窗口可能受到对话框 parent、位置或置顶属性影响，没有被限制在屏幕可用区域内。

今天已经帮用户整理了一份 GitHub Issue 正文，包含：

- 环境
- 复现步骤
- 预期行为
- 实际行为
- 截图说明
- 可能原因
- 修复方向

Issue 地址：

`https://github.com/Zao-chen/ZcChat2/issues/new/choose`

## 4. 测试立绘生成与接入

今天尝试生成一个 Q 版测试角色。

用户偏好：

- 小萝莉感
- 奶白长发
- 可爱
- 有可爱头饰
- 不要太立体
- 更偏 2D，甚至可以像素风

生成结果用户觉得仍然有些立体，但先作为测试对象使用。

已经做了绿幕抠图，生成透明 PNG。

项目默认配置中已更新：

```text
E:\File\AIuseing\xai\ZcChat2\res\default_config\ZcChat2\Character\Assets\test\Tachie\default.png
E:\File\AIuseing\xai\ZcChat2\res\default_config\ZcChat2\Character\Assets\test\Tachie\test_chibi.png
```

运行时用户目录也已同步：

```text
C:\Users\xingxixia\Documents\ZcChat2\Character\Assets\test\Tachie\default.png
C:\Users\xingxixia\Documents\ZcChat2\Character\Assets\test\Tachie\test_chibi.png
```

检查结果：

- PNG
- RGBA
- 尺寸：`1024x1536`
- 左上角像素 alpha 为 0，说明背景透明。

## 5. 立绘大小与聊天框大小

立绘大小由角色用户配置控制。

运行时配置：

```text
C:\Users\xingxixia\Documents\ZcChat2\Character\UserConfig\test\config.json
```

当前内容：

```json
{
    "modelSelect": "deepseek-v4-flash",
    "serverSelect": "DeepSeek",
    "tachieSize": "20"
}
```

核心代码：

`windows/tachie/tachie.cpp`

```cpp
NowTachie.scaled(NowTachie.size() * (safeSize / 100.0),
                 Qt::KeepAspectRatio, Qt::SmoothTransformation);
```

含义：

- `100` 是原图大小。
- `50` 是一半大小。
- `200` 是两倍大小。

聊天框大小目前没有设置项，默认写在：

`windows/dialog/dialog.ui`

```xml
<width>650</width>
<height>200</height>
```

## 6. 产品方向理解

今天讨论后，对这个项目的定位更清楚了。

它不是要做一个复杂的 AI 工具平台，而更像是：

```text
Galgame 风格桌面角色 + AI 对话
```

核心体验应该是：

- 桌面上有一个角色。
- 角色能陪用户聊天。
- 对话框像 Galgame。
- 角色有固定人设和语气。
- 可以顺便帮用户解释代码、学习、陪伴。

所以后续二开方向不应该一开始堆很多功能，而是先把最核心的体验做好：

1. 人物好看。
2. 聊天框舒服。
3. 回复像角色。
4. 启动和窗口交互自然。
5. 基础配置稳定。

## 7. API Key 配置后不回话

用户配置了 API Key，但角色不回话。

今天做了初步排查。

配置状态：

`C:\Users\xingxixia\Documents\ZcChat2\config.json`

- DeepSeek API Key 已存在。
- 未打印 key 原文。
- 只确认 key 长度为 35。

当前角色配置：

```json
{
    "modelSelect": "deepseek-v4-flash",
    "serverSelect": "DeepSeek",
    "tachieSize": "20"
}
```

当前选择角色：

```ini
[character]
CharSelect=test
```

发现一个高概率问题：

`Dialog::ReloadAIConfig()` 原本只刷新：

- serviceType
- model

但没有根据当前服务商重新读取 API Key。

原来的构造函数里有硬编码逻辑：

```cpp
QString apiKey = config.value("llm/DeepSeek/ApiKey").toString();
ai->setApiKey(apiKey); // Todo: 根据不同服务类型设置不同的Key
```

这会导致：

- 如果程序启动后才在设置页填 API Key，已创建的 `Dialog` 可能还拿着旧 key。
- 如果切换 OpenAI/DeepSeek，key 也可能不匹配。

今天最后临时修改了：

`E:\File\AIuseing\xai\ZcChat2\windows\dialog\dialog.cpp`

```cpp
void Dialog::ReloadAIConfig()
{
    /*AI初始化*/
    ZcJsonLib CharConfig(ReadCharacterUserConfigPath());
    //读取当前角色的服务商选择
    QString serverSelect = CharConfig.value("serverSelect").toString();
    if (serverSelect == "DeepSeek")
        ai->setServiceType(AiProvider::DeepSeek);
    else if (serverSelect == "OpenAI")
        ai->setServiceType(AiProvider::OpenAI);
    else
    {
        serverSelect = "DeepSeek";
        ai->setServiceType(AiProvider::DeepSeek);
    }
    ZcJsonLib config(JsonSettingPath);
    ai->setApiKey(config.value("llm/" + serverSelect + "/ApiKey").toString());
    //读取当前角色的模型选择
    QString modelSelect = CharConfig.value("modelSelect").toString();
    ai->setModel(modelSelect);

    loadContextHistory();
}
```

注意：

这个修改还没有重新构建验证。

## 明天继续

明天第一步：

重新 Release 构建。

```powershell
cd E:\File\AIuseing\xai\ZcChat2
& cmd.exe /c 'call "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat" && "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe" --build .\build-release --config Release'
```

然后测试：

1. 启动 `build-release\ZcChat2.exe`
2. 右键立绘打开聊天框
3. 输入一句简单话
4. 按 Enter
5. 看是否有回复或错误提示

如果仍然不回话，继续排查：

- `AiProvider::errorOccurred` 有没有触发。
- DeepSeek 是否返回了错误。
- AI 是否返回了普通文本而不是 `心情|中文|日语`。
- `replyReceived` 是否拿到了内容，但 `section('|', 1, 1)` 解析为空。
- 流式回复是否因为没有 `|` 分隔符导致界面一直不更新。

特别注意：

当前 `test` 角色的 `prompt` 为空。

虽然代码在每次发送前会补系统提示词，要求模型输出：

```text
心情|中文|日语
```

但如果模型没有严格遵守格式，界面可能会显示为空或异常。

后续可以给默认角色写一个更强的 prompt，保证输出稳定。

---

## 8. ??????????????

??????????????????????????

?????

- `windows/dialog/dialog.h`
- `windows/dialog/dialog.cpp`
- `windows/dialog/history/historychild.cpp`
- `windows/setting/child/settingchild_text.h`
- `windows/setting/child/settingchild_text.cpp`
- `windows/setting/setting.cpp`
- `CMakeLists.txt`

?????

1. ???????????????????????????? `QTextEdit`?
   - `ui->textEdit` ????????
   - `m_replyTextEdit` ?????/AI ?????
   - ??????????????? Enter ??????????????

2. ???????
   - ???? Enter??????????/????????????
   - ???? Enter??????????
   - Shift+Enter??????
   - ????????????????????????????????
   - Esc???????

3. ???????????
   - ??????????????????
   - ?????????????????
   - ?????????????????????

4. ????????
   - ?????????????????
   - ??????????????
   - ???????
   - ????/????????????/?? Galgame ??????
   - ???????? `Dialog::ReloadTextSettings()` ??????????

5. ?? AI ??????????????
   - ????? AI ???? `??|????|??????`?
   - ??????????????`????` ???????????????
   - ?????????????????????????????

6. ???? `Dialog::ReloadAIConfig()`?
   - ????????? `serverSelect` ?? DeepSeek/OpenAI?
   - ????????????? API Key?
   - ??????????????
   - ??????????

## 9. ?????? bug ??

?????

- `main.cpp`
- `windows/setting/setting.cpp`
- `windows/tachie/tachie.cpp`

?????

1. ??????????????????
   - `Dialog dialogWin;` ????????????????????
   - `dialogWin.show();` ????????
   - ???????????????/??????

2. ???????????
   - ???????????????????????????????????
   - ??????????????????
   - ????????????????????
   - ????????????????

3. ?????????
   - ?? AI ??????????????? `.png/.jpg/.jpeg`?
   - ??????????????
   - ???????????????????????????????
   - ??/???????????????????????????

## 10. ????????

?????????? Q ?????????? `test` ????????

?????

- `res/default_config/ZcChat2/Character/Assets/test/Tachie/default.png`
- `res/default_config/ZcChat2/Character/Assets/test/Tachie/test_chibi.png`
- `res/default_config/ZcChat2/Character/Assets/test/Tachie/test_chibi_source.png`

?????

- ??????????? ChatGPT Image 2 ???
- ?????????????????????
- ?????????????????????????

## 11. AI ?????????

????????? AI ????????????????????????????????????????????????????? Release ?????

???????? Zao-chen ? GPL-3.0 ???? ZcChat2 ???

- ??????`https://github.com/Zao-chen/ZcChat2`
- ??? `LICENSE`?
- ??? README ???????
- ??????????????
- ????????????
- ?? `AI_DISCLOSURE.md` ?? AI ????????????

## 12. ?????

???????

1. Release ?????

```powershell
cd E:\File\AIuseing\xai\ZcChat2
& cmd.exe /c 'call "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat" && "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe" --build .\build-release --config Release'
```

2. ?????? UTF-8 ?????

?????

- `windows/dialog/dialog.cpp`
- `windows/dialog/dialog.h`
- `windows/setting/child/settingchild_text.cpp`

3. ????? API Key ???????????

????????????????????????????????? API Key?

---

# XChat1 第二版变更日志 - 2026-05-15

第二版在第一版基础上继续收束为“单个桌宠个体”的体验，重点补齐提示词配置和历史对话维护能力。

## 相对第一版解决的问题

- 修正聊天框隐藏后，历史对话窗口仍停留在桌面上的问题。
- 修正设置页修改历史后，聊天框内存历史没有同步刷新的问题。
- 修正历史维护页部分文字和按钮显示过窄、内容溢出的问题。

## 相对第一版新增的功能

- 新增“桌宠行为设定”文本框，可在设置界面维护桌宠基础提示词。
- AI 调用时优先读取当前桌宠配置中的 `petPrompt`。
- 隐藏多角色选择、导入、导出、删除入口，界面方向收束为固定桌宠个体。
- 在角色设置底部增加页码切换。
- 新增历史对话维护页，支持刷新、清空、编辑保存、删除、删除一轮和回滚。
- 历史维护页用“你 / 她 / 记录”和不同背景色区分用户、角色和异常记录。

## 发布说明

- 第二版继续遵守原项目 GPL-3.0 协议。
- README 中继续说明本项目基于 ZcChat2 二次开发，不声明为原作者官方版本。
- `runtime_data/` 不提交到仓库，避免泄露本地 API Key 和聊天历史。
- 本项目说明中保留 AI 辅助开发和 ChatGPT Image 2 生成测试素材的标注。

